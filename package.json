{
  "private": true,
  "name": "{{ project-name }}",
  "version": "0.0.0",
  "description": "A template for kick starting a Cloudflare Workers project with Grain",
  "main": "index.js",
  "author": "{{ authors }}",
  "scripts": {
    "wrangler": "wrangler"
  },
  "dependencies": {
    "@wasmer/wasi": "^0.12.0",
    "@wasmer/wasmfs": "^0.12.0"
  },
  "devDependencies": {
    "@cloudflare/wrangler": "^1.19.2",
    "copy-webpack-plugin": "^5.1.2",
    "webpack": "^4.46.0"
  }
}
